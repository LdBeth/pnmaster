 mapcolor←{
    ⍝ x y←mapcolor r g b
    ⍝ Assume 3×4096×4096 CLUT
     ⎕IO←0
     ⍺←⍬
     r←0⌷⍵ ⋄ g←1⌷⍵ ⋄ b←2⌷⍵
     y←4096|¯1+r+256×16|g
     x←(⌊g÷16)+b×16
     3 4096 4096≢⍴⍺:x y ⍝ if no map
     ⍺[;x;y]
 }
